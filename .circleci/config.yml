version: 2
jobs:
  build:
    docker:
      - image: gcr.io/cloud-builders/dotnet

    steps:
      - checkout

      - run:
          name: Greeting
          command: ls -l

      - run:
          name: Print the Current Time
          command: date
